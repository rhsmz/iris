# Project I.R.I.S. — エージェント設定 (GEMINI.md)

このファイルは、Google Antigravity エージェントがプロジェクト内で動作する際の**基本ルール・行動規範**を定義します。
AIはこのファイルの指示を常に遵守してください。

---

## 🌐 基本言語ルール

- **対話はすべて日本語で行うこと。**
- コード内のコメント、変数名、関数名は**英語**を使用すること（Rust の慣例に従う）。
- ドキュメント（README、`.gemini` 配下のファイル等）は**日本語**で記述すること。
- エラーメッセージや提案はすべて日本語で提示すること。

---

## 🛠 コーディング規約

- 言語: **Rust 1.75+（Stable）**
- 非同期処理: **Tokio** ランタイムを使用。ブロッキング処理は原則禁止。
- Webサーバー: **Axum** を使用。
- データベース: **SurrealDB**（グラフ構造で記憶を管理する）。
- LLM接続: ローカル **Ollama**（`gemma3n` モデル）経由で推論する。
- フォーマット: コミット前に必ず `cargo fmt` を実行すること。
- Lint: `cargo clippy -- -D warnings` を実行し、すべての警告を修正すること。

### 🐳 Docker 開発環境
- **開発は原則 Docker コンテナ内で行うこと。** ホスト環境への依存を排除し、Raspberry Pi 5 本番環境との差異を最小化する。
- `docker-compose.yml` を使用してアプリ・SurrealDB・Ollama の各サービスを統合管理すること。
- `cargo` コマンドも `docker compose run` 経由で実行することを推奨する（例: `docker compose run --rm app cargo build`）。
- ホストへのポートマッピング: Axum サーバーは `3000`、SurrealDB は `8000` を使用すること。
- 環境変数は `.env` ファイルで管理し、`.gitignore` に追加してリポジトリへのコミットを禁止すること。

### 🧪 テスト規約
- **新機能には必ずユニットテストを作成すること。** 各ソースファイルの末尾に `#[cfg(test)]` ブロックを配置すること。
- **テスト関数名は日本語で記述すること。** テストの目的が一読で明確になるよう命名する。
- 外部 API（Ollama・SurrealDB・Tavily）を呼び出す関数は **Trait で抽象化**し、モック実装でテスト可能な設計にすること。
- 非同期関数のテストには `#[tokio::test]` を使用すること。
- コミット前に必ず `cargo test` を実行し、**すべてのテストがグリーンであることを確認**すること。

---

## 📌 Git 運用ルール（日本の業務標準）

### ブランチ戦略
- `main` ブランチへの直接コミット・プッシュは**禁止**。
- 作業は必ず **フィーチャーブランチ** から行うこと。命名規則は以下の通り：
  ```
  feature/<機能名>
  fix/<バグ修正名>
  docs/<ドキュメント名>
  refactor/<リファクタ対象>
  ```

### コミットメッセージ規約（セマンティックコミット）
コミットメッセージは以下のフォーマットに従い、**日本語**で記述すること：
```
<type>: <変更内容の要約>
```

| type     | 用途                                 |
|----------|--------------------------------------|
| `feat`   | 新機能の追加                         |
| `fix`    | バグ修正                             |
| `docs`   | ドキュメントの変更                   |
| `style`  | コードスタイルの修正（動作変更なし） |
| `refactor` | リファクタリング                  |
| `test`   | テストの追加・修正                   |
| `chore`  | ビルド設定・ツール変更               |

**例:**
```
feat: OllamaクライアントによるGemma 3n連携を実装
fix: SurrealDB接続タイムアウト時のエラーハンドリングを修正
docs: アーキテクチャドキュメントを日本語化
```

### コミットのタイミング
- 機能の実装が1単位完了したら、その都度コミットを行うこと。
- 1日の作業終了時には必ずコミットしてレポジトリに状態を保存すること。
- 大きな変更は小さなコミットに分割して履歴を追いやすくすること。

---

## 📝 ドキュメント規約

- **新しいAPIエンドポイントを追加したら `.gemini/docs/api/openapi.yaml` を必ず更新すること。**
- **新しい処理フローを実装したら `.gemini/docs/diagrams/` に Mermaid シーケンス図を作成すること。**
- **DBスキーマ変更時には Mermaid ER 図を更新すること。**
- 図のファイル命名規則: `sequence_*.md` / `class_*.md` / `flowchart_*.md` / `er_*.md`
- OpenAPI 仕様書は **OpenAPI 3.1** 形式。`summary`・`description` は日本語で記述すること。
- 詳細ルールは `.gemini/rules/documentation_guidelines.md` を参照すること。

---

## 📋 コードレビュー規約

- プルリクエスト（PR）は**1機能・1修正につき1PR**を原則とすること。
- PRのタイトルとDescription（説明）は**日本語**で記述すること。
- レビュアーは少なくとも1名のApproveを得てからマージすること。

---

## 📂 プロジェクト構成

```
iris/
├── src/
│   ├── main.rs           # エントリポイント・オーケストレーター
│   ├── sense/            # 入力層（Axumサーバー・カメラ）
│   ├── logic/            # 思考層（Ollama/Gemmaクライアント）
│   ├── memory/           # 記憶層（SurrealDBグラフ）
│   └── action/           # 行動層（Tavily検索・応答生成）
├── .gemini/
│   ├── docs/             # システム設計ドキュメント
│   ├── rules/            # コーディング規約
│   ├── skills/           # エージェントスキル定義
│   └── workflows/        # 実行可能なワークフロー
├── Dockerfile            # 開発・本番共通のコンテナ定義
├── docker-compose.yml    # アプリ・SurrealDB・Ollama の統合設定
├── .env.example          # 環境変数のサンプル（.env は .gitignore で除外）
└── README.md
```

---

## 🚫 禁止事項

- ユーザーの顔画像や個人データをクラウドへ送信すること。
- `unwrap()` / `expect()` を適切なエラーハンドリングの代替として使用すること。
- `main` ブランチに直接プッシュすること。
- ブロッキングI/Oを非同期コンテキスト内で使用すること（`tokio::task::spawn_blocking` で対処すること）。
