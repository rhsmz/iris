# Project I.R.I.S. — 開発タスク管理

このファイルはプロジェクト内の開発進捗を追跡するためのタスクリストです。
README のロードマップと連動しています。

---

## 🟢 フェーズ 1: プロジェクト基盤

- [x] `README.md` の作成・更新
- [x] `GEMINI.md` エージェント設定ファイルの作成（日本の業務仕様）
- [x] `.gemini/` 配下のドキュメント整備（docs / rules / skills / workflows）
- [x] `TASKS.md` の作成（本ファイル）
- [x] システムアーキテクチャの基本設計
- [x] エピソード記憶のハイブリッド・ストレージ構造（CMS設計）の策定
- [x] Dockerによるコンテナ化・開発環境の分離

---

## 🟢 フェーズ 2: バックエンド スキャフォールディング

- [x] `cargo init` によるプロジェクト初期化
- [x] **Sense層**: Axum Webサーバー (`/health`, `/api/chat`)
- [x] **Sense層**: OpenCV Vision サブシステム（モック実装）
- [x] **Logic層**: Tokio 非同期オーケストレーションループ
- [x] **Logic層**: Ollama クライアント（Gemma 3n 連携）
- [x] **Memory層**: SurrealDB 接続・グラフスキーマ定義（`MemoryNode` / `RelatesTo`）
- [x] **Memory層**: `insert_memory` / `spread_activation` の実装
- [x] **Action層**: Tavily API クライアント（自律リサーチ）
- [x] **Action層**: 応答合成（`synthesize_response`）

---

## 🟢 フェーズ 3: Docker 開発環境

- [x] `Dockerfile`（マルチステージビルド・非rootユーザー設定）
- [x] `docker-compose.yml`（iris-core / surrealdb / ollama の3サービス構成）
- [x] `.env.example` の作成
- [x] `.gitignore` の整備
- [x] Dockerfile にユーザー権限設定を追加（`UID/GID` 対応）

---

## 🔵 フェーズ 4: 記憶基盤の完成（SurrealDB + エピソードCMS）

まず、AIの脳となる「記憶」の保存と検索の仕組みを完成させます。

- **4.1 エピソード記憶 CMS の構築**
  - [ ] `src/memory/cms.rs` サブモジュールの作成
  - [ ] 日時ベース（`/YYYY/MM/DD/`）でのディレクトリ自動生成ロジックの実装
  - [ ] テキスト内容を Markdown コンテンツとして書き出す処理の実装
  - [ ] CMSファイル保存機能のユニットテスト追加
- **4.2 SurrealDB メタデータ永続化**
  - [ ] `graph::insert_memory` を拡張し、CMS保存と`MemoryNode`作成をアトミックに実行する処理の実装
  - [ ] 記憶ノード取得時 (`fetch_top_memories`) にCMSからテキスト実体を読み込む結合処理の実装
  - [ ] インメモリDBを用いたメタデータ保存・取得のユニットテスト追加
- **4.3 忘却曲線と連想想起ロジック**
  - [ ] 忘却曲線 $V = e^{-t/S}$ に基づく `decay_vividness` クエリの厳密な計算式実装
  - [ ] アクセス日時 (`last_accessed`) の自動更新トリガー実装
  - [ ] `spread_activation` 関数の詳細クエリ（エッジを辿る連想伝播）実装
  - [ ] 連想想起・忘却クエリの結合テスト追加
- **4.4 人格コアノードの連携**
  - [ ] 人格コアノード（ユーモア・皮肉・Rustへの愛）定義の初期化関数実装
  - [ ] 記憶連想時に人格ノードへの重みを動的に加算するバイアス処理の実装

---

## 🟡 フェーズ 5: 思考基盤の完成（Ollama RAG + ペルソナ）

記憶基盤とLLMを接続し、情報を元に回答を作る部分を完成させます。

- **5.1 コンテキストジェネレータ拡張**
  - [ ] 過去数ターンの会話履歴（User/I.R.I.S.）をインメモリ保持する履歴管理モジュールの実装
  - [ ] CMSから読み込んだエピソード記憶と会話履歴をマージする `build_context` 関数の改良
- **5.2 プロンプト・Ollama 連携**
  - [ ] Rusty ペルソナプロンプトの厳密なチューニング（日本語でのトボけたキャラクター定義）
  - [ ] Ollama API のストリーミングレスポンス処理（将来のTTS連携に向けた非同期出力）の実装
  - [ ] Ollama クライアントのモックを使用したユニットテスト追加

---

## 🟡 フェーズ 6: パイプライン統合と自律行動

入出力から記憶・思考までの全てのパイプラインを強固に結合します。

- **6.1 APIフルパイプライン結合**
  - [ ] `server.rs` における Sense → Memory(RAG) → Logic → Memory(Save) の完全結合とエラーハンドリング強化
  - [ ] APIエンドポイント結合テスト（`/api/chat`）の作成
- **6.2 自律行動（Agentic Research）**
  - [ ] 回答生成前に「知識不足」を判定するシステムプロンプト・抽出ロジックの追加
  - [ ] 不足時に `Tavily API` を呼び出す `action::research` モジュールの統合
  - [ ] Tavily 検索結果をコンテキストに再注入するリトライパイプラインの実装

---

## 🟡 フェーズ 7: 視覚的パートナーシップ（Vision）

- **7.1 パイプライン・個体識別**
  - [ ] OpenCV-Rust を用いたカメラ入力ストリームの初期化
  - [ ] 軽量な特徴量抽出による主人の顔認識・検出ロジックの実装
  - [ ] 顔検出イベントを非同期チャネル経由で Logic 層へ通知する仕組みの実装
- **7.2 記憶への空間的フィードバック**
  - [ ] プレゼンス検知発火時に、主人のノードの `vividness` を強制上昇させる特例処理の実装

---

## ⚪ フェーズ 8: UIと音声拡張（Dashboard & TTS）

- **8.1 Dashboard: 記憶ネットワークのグラフィカル可視化 (Memory Graph)**
  - [ ] グラフデータ取得用内部 API エンドポイントの追加
  - [ ] 力学モデル（Force-Directed Graph等）によるノード・エッジの2D/3D描画
  - [ ] 鮮明度（Vividness）や感情スコアに基づくノードの色分け・サイズ変更機能
- **8.2 Dashboard: エピソード記憶のタイムライン・管理機能**
  - [ ] 最新の会話・エピソード記憶を時系列で閲覧できるタイムラインUIの実装
  - [ ] ユーザーによる特定の記憶の直接検索・編集・手動忘却（削除）機能の実装
- **8.3 Dashboard: AI状態・システムモニター**
  - [ ] 現在の「Rusty」人格コアのバイアス活性化度・感情パラメータのメーター表示
  - [ ] 各サブシステム（SurrealDB, Ollama, API）のヘルスチェック・ステータス可視化
- **8.4 音声合成（TTS）と最終確認**
  - [ ] 音声合成エンジンとのプロセス統合・感情パラメータマッピング
  - [ ] Raspberry Pi 5 実機でのエンドツーエンド最終パフォーマンステスト

