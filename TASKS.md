# Project I.R.I.S. — 開発タスク管理

このファイルはプロジェクト内の開発進捗を追跡するためのタスクリストです。
README のロードマップと連動しています。

---

## 🟢 フェーズ 1: プロジェクト基盤

- [x] `README.md` の作成・更新
- [x] `GEMINI.md` エージェント設定ファイルの作成（日本の業務仕様）
- [x] `.gemini/` 配下のドキュメント整備（docs / rules / skills / workflows）
- [x] `TASKS.md` の作成（本ファイル）
- [x] システムアーキテクチャの基本設計
- [x] エピソード記憶のハイブリッド・ストレージ構造（CMS設計）の策定
- [x] Dockerによるコンテナ化・開発環境の分離

---

## 🟢 フェーズ 2: バックエンド スキャフォールディング

- [x] `cargo init` によるプロジェクト初期化
- [x] **Sense層**: Axum Webサーバー (`/health`, `/api/chat`)
- [x] **Sense層**: OpenCV Vision サブシステム（モック実装）
- [x] **Logic層**: Tokio 非同期オーケストレーションループ
- [x] **Logic層**: Ollama クライアント（Gemma 3n 連携）
- [x] **Memory層**: SurrealDB 接続・グラフスキーマ定義（`MemoryNode` / `RelatesTo`）
- [x] **Memory層**: `insert_memory` / `spread_activation` の実装
- [x] **Action層**: Tavily API クライアント（自律リサーチ）
- [x] **Action層**: 応答合成（`synthesize_response`）

---

## 🟢 フェーズ 3: Docker 開発環境

- [x] `Dockerfile`（マルチステージビルド・非rootユーザー設定）
- [x] `docker-compose.yml`（iris-core / surrealdb / ollama の3サービス構成）
- [x] `.env.example` の作成
- [x] `.gitignore` の整備
- [x] Dockerfile にユーザー権限設定を追加（`UID/GID` 対応）

---

## 🔵 フェーズ 4: 記憶基盤の完成（SurrealDB + エピソードCMS）

まず、AIの脳となる「記憶」の保存と検索の仕組みを最小単位の関数レベルで構築します。

- **4.1 エピソード記憶 CMS の基礎実装**
  - [ ] `src/memory/cms.rs` ファイルの作成とモジュール登録
  - [ ] 環境変数 `MEMORIES_PATH` の読み込みとディレクトリ作成ロジックの実装
  - [ ] タイムスタンプから `YYYY/MM/DD` 形式の階層パスを生成する関数の実装
  - [ ] 指定文字列をMarkdownファイルとして保存する `save_markdown` 関数の実装
  - [ ] パスを受け取りテキスト内容を読み込む `load_markdown` 関数の実装
  - [ ] CMS保存・読み込みの完全なライフサイクル単体テストの作成

- **4.2 SurrealDB メタデータ永続化拡張**
  - [ ] `MemoryNode` 構造体にエピソードファイルへの絶対パスを保持する `file_path` フィールドを追加
  - [ ] `MemoryNode` 構造体にAIの主観的感情を保持する `emotion_score` フィールドを追加
  - [ ] 既存の `insert_memory` を改修し、CMSの `save_markdown` を呼び出してパスを取得・結合する処理の実装
  - [ ] デバッグ用インメモリSurrealDBを用いた `insert_memory` の機能テストの作成

- **4.3 連想想起ロジックと忘却曲線**
  - [ ] 忘却曲線式（$V = e^{-t/S}$）に基づく鮮明度（`vividness`）減衰計算関数の実装
  - [ ] `MemoryNode` の `last_accessed` を現在日時に更新する専用クエリ関数の実装
  - [ ] `fetch_top_memories` を改修し、`vividness` と `emotion_score` の合計値によるソートクエリを実装
  - [ ] 上位ノードの `file_path` を用いてCMSからテキストを自動で読み込むマージ処理の実装
  - [ ] グラフエッジ（`RelatesTo`）を辿って関連ノードを取得する `spread_activation` クエリの実装
  - [ ] 連想伝播と忘却曲線の結合単体テストを作成・実行

- **4.4 人格コアノードの連携**
  - [ ] 「皮肉」「ユーモア」「Rustへの愛」を持つ特殊な `PersonalityNode` の定義
  - [ ] システム起動時に `PersonalityNode` をSurrealDBへ初期投入（Upsert）する関数の実装
  - [ ] 記憶連想時に人格コアへのエッジを持つノードへの重み加算クエリの実装

---

## 🟡 フェーズ 5: 思考基盤の完成（Ollama RAG + ペルソナ）

記憶基盤とLLMを接続し、情報を元に回答を作る部分を完成させます。

- **5.1 履歴コンテキスト管理**
  - [ ] 過去Nターンの会話履歴（UserとI.R.I.Sのペア）を保持するインメモリ構造体の実装
  - [ ] コンテキストサイズ上限に達した際に古い会話を切り捨てるFIFOロジックの実装
- **5.2 プロンプトジェネレーター構築**
  - [ ] `src/logic/prompt.rs` モジュールの作成
  - [ ] "Rusty" な基本人格を定義する日本語のシステムプロンプト文字列の作成
  - [ ] システムプロンプト＋過去記憶＋会話履歴を単一文字列にマージする関数の実装
  - [ ] プロンプト生成ロジックの単体テストの作成
- **5.3 Ollama 推論連携**
  - [ ] `src/logic/ollama.rs` モジュールの作成と `reqwest` クライアント初期化
  - [ ] Ollama ローカルAPI `/api/generate` へのHTTPリクエスト送信ロジックの実装
  - [ ] Ollamaのストリーミングレスポンスを受け取り、非同期でチャンクをパースする関数の実装

---

## 🟡 フェーズ 6: パイプライン統合と自律行動

入出力から記憶・思考までの全てのパイプラインを強固に結合します。

- **6.1 APIフルパイプライン結合**
  - [ ] `src/sense/server.rs` 内の `/api/chat` エンドポイントルーター実装
  - [ ] 受信テキスト → Logic(推論) → Memory(保存) を繋ぐ一連の非同期呼び出しの実装
  - [ ] 例外・エラーハンドリングの強化（適切なHTTP 500等のステータス返却）
  - [ ] クライアントへの推論結果のストリーミングレスポンス (SSE) の実装
  - [ ] `axum::TestClient` を用いたエンドツーエンドのWeb API統合テスト作成
- **6.2 自律的リサーチ機能（Tavily連携）**
  - [ ] `src/action/research.rs` クライアントの実装（Tavily APIキー環境変数読込）
  - [ ] Ollamaのシステムプロンプトに「検索が必要な場合の特殊フォーマット命令」を追加
  - [ ] Ollama推論出力から検索要求タグを正規表現またはJSONパースで抽出する処理の実装
  - [ ] 抽出クエリでTavily APIへリクエストを送り、結果を取得する関数の実装
  - [ ] Tavilyの検索結果を再度RAGコンテキストに注入し、推論をやり直すリトライループの実装

---

## 🟡 フェーズ 7: 視覚的パートナーシップ（Vision）

カメラを利用して「部屋にいる」という空間的認識を追加します。

- **7.1 パイプライン・個体識別**
  - [ ] `Cargo.toml` への `opencv` または `video4linux` 関連クレート追加
  - [ ] `src/sense/vision.rs` モジュールの作成
  - [ ] `/dev/video0` を非同期タスクで定期監視するOpenCVループ処理の実装
- **7.2 顔認識・プレゼンス検知**
  - [ ] 軽量な顔検出（Haar Cascades等）を用いた画像フレーム解析関数の実装
  - [ ] 顔検出イベントを Tokio `mpsc` チャネル経由でメインループへ発火する機構の実装
  - [ ] イベント受信時、SurrealDBの主人のノードの `vividness` を強制上昇させるクエリの発行処理の実装

---

## ⚪ フェーズ 8: UIと音声拡張（Dashboard & TTS）

ダッシュボードや音声機能を拡張します。

- **8.1 内部可視化API構築**
  - [ ] SurrealDBの全ノードとエッジリストを取得する `GET /api/graph` エンドポイントの実装
  - [ ] 現在稼働中の各人格バイアスメーターを取得する `GET /api/status` エンドポイントの実装
- **8.2 Leptos ダッシュボード基盤（フロントエンド）**
  - [ ] 新規クレートとしてフロントエンド向け Leptos プロジェクトのセットアップ
  - [ ] 描画ライブラリ（d3.js または Force-Graph）の取り込みとラッパーコンポーネント作成
  - [ ] 力学モデルによる記憶ノード・エッジの描画コンポーネントの実装
  - [ ] Vividnessヒートマップ（色付け）の反映ロジック実装
- **8.3 リアルタイムUI機能**
  - [ ] 最新のエピソード記憶リストを表示するサイドパネルUIの実装
  - [ ] グラフ上のノード押下時に、CMSからテキストを取得・表示する詳細モーダルUIの実装
  - [ ] 特定のノードを手動削除（忘却コマンド）するボタンとAPI呼び出しフロントエンドの実装
- **8.4 音声合成（TTS）基盤**
  - [ ] 軽量TTSエンジン（`espeak-ng` 等）のインストールまたはDocker追加
  - [ ] 生成された解答テキストをストリーム単位でTTSプロセスに渡し、音声を再生する処理の実装
  - [ ] Raspberry Pi 5 実機でのカメラ＋音声＋LLM フル連携最終動作テスト
